diff --git a/config.sub b/config.sub
index 38f3d037a785..ed878f2b784e 100755
--- a/config.sub
+++ b/config.sub
@@ -1735,7 +1735,7 @@ case $os in
 	     | bosx* | nextstep* | cxux* | aout* | elf* | oabi* \
 	     | ptx* | coff* | ecoff* | winnt* | domain* | vsta* \
 	     | udi* | lites* | ieee* | go32* | aux* | hcos* \
-	     | chorusrdb* | cegcc* | glidix* | serenity* \
+	     | chorusrdb* | cegcc* | glidix* | anos* | serenity* \
 	     | cygwin* | msys* | pe* | moss* | proelf* | rtems* \
 	     | midipix* | mingw32* | mingw64* | mint* \
 	     | uxpv* | beos* | mpeix* | udk* | moxiebox* \
diff --git a/fixincludes/mkfixinc.sh b/fixincludes/mkfixinc.sh
index 7112f4dcd64b..fb18c0dd7dbb 100755
--- a/fixincludes/mkfixinc.sh
+++ b/fixincludes/mkfixinc.sh
@@ -11,6 +11,8 @@ target=fixinc.sh
 
 # Check for special fix rules for particular targets
 case $machine in
+    *-anos* | \
+    *-*-anos* | \
     i?86-*-cygwin* | \
     *-mingw32* | \
     powerpc-*-eabisim* | \
diff --git a/gcc/config.gcc b/gcc/config.gcc
index 087aaa7be371..b5a0a4aafd90 100644
--- a/gcc/config.gcc
+++ b/gcc/config.gcc
@@ -723,6 +723,13 @@ x86_cpus="generic intel"
 
 # Common parts for widely ported systems.
 case ${target} in
+*-*-anos*)
+  extra_options="$extra_options gnu-user.opt"
+  gas=yes
+  gnu_ld=yes
+  default_use_cxa_atexit=yes
+  use_gcc_stdint=provide
+  ;;
 *-*-darwin*)
   tmake_file="t-darwin "
   tm_file="${tm_file} darwin.h"
@@ -1186,6 +1193,12 @@ case ${target} in
 esac
 
 case ${target} in
+i[34567]86-*-anos*)
+    tm_file="${tm_file} i386/unix.h i386/att.h elfos.h glibc-stdint.h i386/i386elf.h anos.h gnu-user.h"
+    ;;
+x86_64-*-anos*)
+    tm_file="${tm_file} i386/unix.h i386/att.h elfos.h glibc-stdint.h i386/i386elf.h i386/x86-64.h anos.h gnu-user.h"
+    ;;
 aarch64*-*-elf | aarch64*-*-fuchsia* | aarch64*-*-rtems*)
 	tm_file="${tm_file} elfos.h newlib-stdint.h"
 	tm_file="${tm_file} aarch64/aarch64-elf.h aarch64/aarch64-errata.h aarch64/aarch64-elf-raw.h"
@@ -1960,6 +1973,7 @@ i[34567]86-*-elf*)
 	tm_file="${tm_file} i386/unix.h i386/att.h elfos.h newlib-stdint.h i386/i386elf.h"
 	;;
 x86_64-*-elf*)
+	tmake_file="${tmake_file} i386/t-x86_64-elf"
 	tm_file="${tm_file} i386/unix.h i386/att.h elfos.h newlib-stdint.h i386/i386elf.h i386/x86-64.h"
 	;;
 x86_64-*-rtems*)
diff --git a/gcc/config/anos.h b/gcc/config/anos.h
new file mode 100644
index 000000000000..cf70804e1b66
--- /dev/null
+++ b/gcc/config/anos.h
@@ -0,0 +1,30 @@
+/* Useful if you wish to make target-specific GCC changes. */
+#undef TARGET_ANOS
+#define TARGET_ANOS 1
+
+/* Disable red zone by default on x86-64 */
+#undef TARGET_DEFAULT
+#define TARGET_DEFAULT (TARGET_DEFAULT | MASK_NO_RED_ZONE)
+
+/* Default arguments for i686-anos-gcc/x86_64-anos-gcc */
+#undef LIB_SPEC
+#define LIB_SPEC "-lc" /* link against C standard library */
+
+/* Files that are linked before user code.
+   The %s tells GCC to look for these files in the library directory. */
+#undef STARTFILE_SPEC
+#define STARTFILE_SPEC "crt0.o%s crti.o%s crtbegin.o%s"
+
+/* Files that are linked after user code. */
+#undef ENDFILE_SPEC
+#define ENDFILE_SPEC "crtend.o%s crtn.o%s"
+
+/* Additional predefined macros. */
+#undef TARGET_OS_CPP_BUILTINS
+#define TARGET_OS_CPP_BUILTINS()      \
+    do {                              \
+      builtin_define ("anos");        \
+      builtin_define ("unix");        \
+      builtin_assert ("system=anos"); \
+      builtin_assert ("system=unix"); \
+    } while(0);
diff --git a/libgcc/config.host b/libgcc/config.host
index 6a88ee5a2dd0..2b845bac07d2 100644
--- a/libgcc/config.host
+++ b/libgcc/config.host
@@ -400,8 +400,15 @@ i[34567]86-*-cygwin* | x86_64-*-cygwin*)
   enable_execute_stack=enable-execute-stack-empty.c;
   ;;
 esac
-
 case ${host} in
+i[34567]86-*-anos*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtend.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
+x86_64-*-anos*)
+	extra_parts="$extra_parts crti.o crtbegin.o crtend.o crtn.o"
+	tmake_file="$tmake_file i386/t-crtstuff t-crtstuff-pic t-libgcc-pic"
+	;;
 aarch64*-*-elf | aarch64*-*-rtems*)
 	extra_parts="$extra_parts crtbegin.o crtend.o crti.o crtn.o"
 	extra_parts="$extra_parts crtfastmath.o"
diff --git a/libstdc++-v3/crossconfig.m4 b/libstdc++-v3/crossconfig.m4
index b3269cb88e07..15d00627d6f7 100644
--- a/libstdc++-v3/crossconfig.m4
+++ b/libstdc++-v3/crossconfig.m4
@@ -67,6 +67,13 @@ case "${host}" in
     AC_DEFINE(HAVE_USELOCALE)
     ;;
 
+  *-anos*)
+    GLIBCXX_CHECK_COMPILER_FEATURES
+    GLIBCXX_CHECK_LINKER_FEATURES
+    GLIBCXX_CHECK_MATH_SUPPORT
+    GLIBCXX_CHECK_STDLIB_SUPPORT
+    ;;
+
   *-darwin*)
     # Darwin versions vary, but the linker should work in a cross environment,
     # so we just check for all the features here.
